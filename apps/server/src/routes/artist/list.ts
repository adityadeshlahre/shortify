

import type { Context } from "hono";
import factory from "utils/factory";

export const showArtistsList = factory.createHandlers({
	async (c: Context) => {
		try {

		}
	} catch (error) {

		}
	}
			});
